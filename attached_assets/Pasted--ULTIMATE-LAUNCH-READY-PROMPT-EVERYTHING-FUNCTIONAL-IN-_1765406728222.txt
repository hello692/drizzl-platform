🚀 ULTIMATE LAUNCH-READY PROMPT - EVERYTHING FUNCTIONAL IN 2 HOURS
Copy and paste this MASSIVE prompt into Replit Agent:

🚨 CRITICAL MISSION: MAKE EVERYTHING 100% FUNCTIONAL - LAUNCHING IN 2 HOURS

We are going LIVE in 2 hours. EVERY button, EVERY link, EVERY feature must be FULLY FUNCTIONAL.

NO placeholders. NO "coming soon". NO demo buttons. NO broken links.

If a button exists, it MUST work. If a feature is shown, it MUST function.

═══════════════════════════════════════════════════════════════════
🎯 CRITICAL ISSUES TO FIX IMMEDIATELY
═══════════════════════════════════════════════════════════════════

ISSUE #1: MISSING SALES REP AUTH PAGES
ISSUE #2: MISSING MESSAGES/GSUITE INTEGRATION IN ALL DASHBOARDS
ISSUE #3: NON-FUNCTIONAL BUTTONS THROUGHOUT ENTIRE SITE
ISSUE #4: DEMO/PLACEHOLDER CONTENT EVERYWHERE

We will fix ALL OF THESE systematically.

═══════════════════════════════════════════════════════════════════
PART 1: ADD SALES REP SIGNUP/LOGIN TO LANDING PAGE
═══════════════════════════════════════════════════════════════════

LOCATION: Homepage (/) - Add a section for sales team

ADD THIS SECTION TO HOMEPAGE (after hero, before features):

┌────────────────────────────────────────────────────────────────┐
│                    JOIN OUR SALES TEAM                          │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Are you a sales representative?                               │
│  Access your dashboard to manage leads and close deals.        │
│                                                                 │
│  [Sales Rep Login] [Apply to Join Our Team]                   │
│                                                                 │
└────────────────────────────────────────────────────────────────┘

OR add to main navigation:

┌────────────────────────────────────────────────────────────────┐
│ DRIZZL    Shop | Wholesale | For Sales Reps | About | Contact │
└────────────────────────────────────────────────────────────────┘

CREATE THESE PAGES:

1. /sales/login
   - Email input
   - Password input
   - [Sign In] button → MUST actually authenticate
   - "Forgot password?" link → goes to /sales/forgot-password
   - NO "Sign up" link (sales reps created by admin only)

2. /sales/forgot-password
   - Email input
   - [Send Reset Link] button → MUST send real email via Resend
   - Success message: "Check your email for reset instructions"
   - Back to login link

3. /sales/reset-password?token=xxx
   - New password input
   - Confirm password input
   - [Reset Password] button → MUST update password in database
   - Success: redirect to /sales/login with success message

AUTHENTICATION MUST WORK:
- Use Supabase Auth for sales reps
- Create sales_reps table if not exists
- Hash passwords properly
- Session management
- Protected routes (redirect to login if not authenticated)

═══════════════════════════════════════════════════════════════════
PART 2: ADD MESSAGES + GSUITE INTEGRATION TO ALL DASHBOARDS
═══════════════════════════════════════════════════════════════════

AFFECTED DASHBOARDS:
1. Sales Rep Dashboard (/sales/dashboard)
2. B2B Partner Dashboard (/partner/dashboard)
3. Admin Command Center (/admin/command-center)

FOR EACH DASHBOARD, ADD:

A. MESSAGES WIDGET (on dashboard page)

┌────────────────────────────────────────────────────────────────┐
│ 💬 Recent Messages                              [View All →]   │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│ [Avatar] Sarah Johnson (Sales Rep)               2m ago        │
│ "Quick question about the Whole Foods deal..."                │
│ [Reply]                                                        │
│                                                                 │
│ [Avatar] Support Team                            1h ago        │
│ "Your order #10145 has shipped!"                              │
│ [View Order]                                                   │
│                                                                 │
│ [Avatar] Mike Chen (Manager)                     3h ago        │
│ "Great work on closing the Target deal! 🎉"                   │
│ [Reply]                                                        │
│                                                                 │
└────────────────────────────────────────────────────────────────┘

B. MESSAGES PAGE (full messaging interface)

/sales/messages
/partner/messages
/admin/command-center/messages

FUNCTIONALITY REQUIRED:

1. DATABASE SCHEMA:
```prisma
model Conversation {
  id              String   @id @default(cuid())
  participant1Type String  // "admin", "sales_rep", "partner"
  participant1Id  String
  participant2Type String
  participant2Id  String
  subject         String?
  lastMessageAt   DateTime @default(now())
  unreadCount1    Int      @default(0)
  unreadCount2    Int      @default(0)
  messages        Message[]
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  
  @@unique([participant1Type, participant1Id, participant2Type, participant2Id])
}

model Message {
  id              String   @id @default(cuid())
  conversationId  String
  conversation    Conversation @relation(fields: [conversationId], references: [id])
  senderType      String   // "admin", "sales_rep", "partner"
  senderId        String
  senderName      String
  message         String   @db.Text
  attachments     String[] // URLs
  isRead          Boolean  @default(false)
  readAt          DateTime?
  createdAt       DateTime @default(now())
}
```

2. REAL-TIME UPDATES:
   - Use polling (every 5 seconds) or WebSocket
   - Show unread count badge
   - Mark as read when viewed
   - Send notification when new message

3. MESSAGE FEATURES:
   - Send text messages (MUST save to database)
   - Attach files (upload to Supabase Storage)
   - Read receipts
   - Typing indicators (optional but nice)
   - Search messages
   - Archive conversations

C. GSUITE INTEGRATION (Gmail in dashboard)

ADD TO EACH DASHBOARD:

┌────────────────────────────────────────────────────────────────┐
│ 📧 Recent Emails (Gmail)                        [View All →]   │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│ [Connect Gmail Account]                                        │
│                                                                 │
│ OR (if connected):                                             │
│                                                                 │
│ 📩 Order Confirmation - Whole Foods              2m ago ●      │
│    "Thank you for your order! Order #10145..."                │
│                                                                 │
│ 📩 Re: Pricing Question                          1h ago        │
│    "Thanks for reaching out. Our pricing for..."               │
│                                                                 │
│ 📩 New Lead: Target Stores                       3h ago ●      │
│    "Interested in wholesale partnership..."                    │
│                                                                 │
└────────────────────────────────────────────────────────────────┘

GSUITE INTEGRATION STEPS:

1. Create Google Cloud Project:
   - Go to console.cloud.google.com
   - Create new project: "Drizzl Wellness"
   - Enable Gmail API
   - Create OAuth credentials

2. Add to Environment Variables:
GOOGLE_CLIENT_ID=your_client_id
GOOGLE_CLIENT_SECRET=your_client_secret
GOOGLE_REDIRECT_URI=https://drizzlwellness.com/api/auth/google/callback

3. Install Google API Client:
```bash
   npm install googleapis
```

4. Create OAuth flow:
   - [Connect Gmail] button → OAuth consent screen
   - User authorizes Gmail access
   - Store refresh token in database
   - Fetch emails via Gmail API

5. Display Emails:
   - Fetch last 10 emails
   - Show sender, subject, snippet
   - Click to open full email
   - Reply functionality
   - Mark as read/unread

6. Email Sync:
   - Poll Gmail API every 5 minutes
   - Store new emails in database (optional, for faster loading)
   - Show unread count badge

ALTERNATIVE (if Gmail integration too complex):
- Just show messages from internal messaging system
- Add "Email Integration Coming Soon" badge
- For launch, internal messaging is enough

═══════════════════════════════════════════════════════════════════
PART 3: FIX ALL NON-FUNCTIONAL BUTTONS - COMPREHENSIVE AUDIT
═══════════════════════════════════════════════════════════════════

SYSTEMATIC CHECK: Go through EVERY page, EVERY button, EVERY link.

IF IT EXISTS, IT MUST WORK.

HOMEPAGE (/)
────────────────────────────────────────────────────────────────

□ [Shop Now] button → Must go to /shop
□ [Add to Cart] buttons on featured products → Must add to cart
□ [Learn More] buttons → Must go to product detail pages
□ Newsletter signup → Must save email to database AND send confirmation
□ Footer links → All must work (Shop, About, Contact, Terms, Privacy, etc.)
□ Social media icons → Must link to real social profiles OR remove if don't exist
□ Navigation links → All must work

SHOP PAGE (/shop)
────────────────────────────────────────────────────────────────

□ Category filters → Must actually filter products
□ Price filters → Must actually filter products
□ Diet filters (Vegan, Organic, etc.) → Must actually filter products
□ Sort dropdown → Must actually sort products
□ Search bar → Must search products by name/description
□ [Add to Cart] buttons → Must add to cart AND show success toast
□ Product cards clickable → Must go to product detail page
□ Pagination → Must work if more than X products
□ [Load More] button → Must load more products

PRODUCT DETAIL PAGE (/shop/[slug])
────────────────────────────────────────────────────────────────

□ Image gallery → Must show all images, click to zoom
□ Quantity selector [- 1 +] → Must increase/decrease
□ [Add to Cart] button → Must add to cart with selected quantity
□ [Buy Now] button → Must add to cart AND go directly to checkout
□ Subscribe checkbox → Must enable subscription
□ Frequency dropdown → Must work if subscription enabled
□ [Subscribe] button → Must create subscription order
□ Reviews section → Must show real reviews from database
□ [Write Review] button → Must open review form (if logged in)
□ Related products → Must show real related products
□ Tabs (Description, Nutrition, Ingredients) → Must switch content

CART PAGE (/cart)
────────────────────────────────────────────────────────────────

□ Quantity [- + ] → Must update quantity in cart
□ [Remove] button → Must remove item from cart
□ Promo code input → Must apply discount if valid code
□ [Apply] button → Must validate and apply promo code
□ Totals → Must calculate correctly (subtotal, shipping, tax, total)
□ [Continue Shopping] → Must go back to /shop
□ [Checkout] button → Must go to /checkout
□ Cart must persist (localStorage or database)
□ Cart count in header → Must show correct count

CHECKOUT PAGE (/checkout)
────────────────────────────────────────────────────────────────

□ Email input → Must validate email format
□ [Sign in] link → Must go to login (if have account)
□ Shipping address form → All fields must validate
□ Shipping method radio buttons → Must select option
□ Payment form (Stripe) → Must load Stripe Elements
□ [Complete Order] button → Must:
  - Validate all fields
  - Process payment via Stripe
  - Create order in database
  - Send confirmation email
  - Redirect to confirmation page
□ Order summary → Must show correct items and totals

ORDER CONFIRMATION PAGE (/order/confirmation/[id])
────────────────────────────────────────────────────────────────

□ Must fetch real order from database
□ Must show order number
□ Must show items ordered
□ Must show totals
□ Must show shipping address
□ [Track Order] button → Must go to tracking page
□ [Continue Shopping] → Must go to /shop
□ If order not found → Show error page

ACCOUNT PAGES (/account/*)
────────────────────────────────────────────────────────────────

/account/login:
□ Email + password → Must authenticate
□ [Sign In] button → Must log in user
□ "Forgot password?" → Must go to /account/forgot-password
□ "Sign up" link → Must go to /account/signup
□ Show errors if wrong credentials

/account/signup:
□ All form fields → Must validate
□ [Create Account] → Must:
  - Create user in database
  - Hash password
  - Send welcome email
  - Auto-login
  - Redirect to /account/dashboard

/account/forgot-password:
□ Email input → Must validate
□ [Send Reset Link] → Must send email with reset link

/account/dashboard:
□ Must show real user data
□ Must show real orders
□ All buttons must work:
  - [View Order]
  - [Track Order]
  - [Reorder]
  - [Write Review]
  - [Update Profile]

/account/orders:
□ Must show all user's orders from database
□ Filters must work
□ Click order → Go to order details
□ [Reorder] → Add items to cart

/account/subscriptions:
□ Must show real subscriptions
□ [Edit] → Open edit modal that actually updates subscription
□ [Pause] → Actually pause subscription in database
□ [Cancel] → Actually cancel subscription

/account/addresses:
□ Must show real addresses from database
□ [Add Address] → Actually save to database
□ [Edit] → Actually update in database
□ [Delete] → Actually delete from database
□ [Set as Default] → Actually update default flag

/account/payment:
□ Must show saved payment methods (via Stripe)
□ [Add Payment Method] → Actually save to Stripe
□ [Remove] → Actually remove from Stripe
□ [Set as Default] → Actually update default

ADMIN COMMAND CENTER (/admin/command-center/*)
────────────────────────────────────────────────────────────────

EVERY tab must be functional:

/admin/command-center/dashboard:
□ All metrics must show REAL data from database
□ All charts must render with REAL data
□ All links must work

/admin/command-center/finance:
□ Mercury account balances → Real or mock (clearly labeled)
□ Recent transactions → Real from database
□ [Pay Now] buttons → Must actually process payment OR show modal
□ [Send Reminder] → Must send email
□ Reports → Must generate real PDFs from data

/admin/command-center/production:
□ All data from database
□ [Start Batch] → Must create production batch
□ [Complete Batch] → Must update status
□ Progress bars → Must reflect real progress

/admin/command-center/supply-chain:
□ Inventory data from database
□ [Reorder] → Must create purchase order
□ Low stock alerts → Real data
□ [Create PO] → Must save to database

/admin/command-center/b2b-pipeline:
□ All leads from database
□ Drag-and-drop → Must update deal stage
□ [Add Lead] → Must open form and save
□ [View Deal] → Must show details
□ [Edit] → Must update in database

/admin/command-center/tasks:
□ All tasks from database
□ [Mark Complete] → Must update status
□ [Add Task] → Must save to database
□ [Assign] → Must update assignee

/admin/command-center/analytics:
□ All charts with real data
□ Filters must work
□ Export buttons must work

/admin/command-center/marketing:
□ All campaigns from database
□ [Create Campaign] → Must save
□ [Edit] → Must update
□ [Delete] → Must delete

/admin/command-center/ai-assistant:
□ Chat must work
□ AI must respond (pattern matching or Claude API)
□ Messages must save to database

B2B PARTNER PORTAL (/partner/*)
────────────────────────────────────────────────────────────────

/partner/login:
□ Must authenticate
□ Must work with real partner accounts

/partner/dashboard:
□ All data must be real
□ All buttons must work

/partner/orders:
□ Show real orders
□ [Reorder] must work

/partner/orders/new:
□ Product catalog from database
□ [Add to Order] must work
□ Shopping cart must work
□ [Place Order] must:
  - Save order to database
  - Create invoice
  - Send confirmation email

/partner/invoices:
□ Show real invoices
□ [Pay Now] must process payment (Stripe)
□ [Download PDF] must generate PDF

/partner/pricing:
□ Show real products with real pricing
□ Tier discounts must calculate correctly
□ [View Details] must work

/partner/marketing-hub:
□ Show real assets
□ [Download] must download file
□ Download counts must increment

/partner/how-to-guide:
□ Show real videos
□ Videos must play
□ Progress tracking must work

/partner/integrations:
□ [Connect] buttons must work (OAuth flows)
□ Show connection status
□ Sync logs must be real

/partner/track-orders:
□ Show real shipments
□ Maps must show real locations
□ Tracking updates must be real

SALES REP PORTAL (/sales/*)
────────────────────────────────────────────────────────────────

/sales/dashboard:
□ All metrics from database
□ Quota progress must calculate correctly
□ Leaderboard must rank correctly
□ Hot leads must sort by score

/sales/leads:
□ Show real leads from database
□ Filters must work
□ [Add Lead] must save
□ Click lead → Go to detail

/sales/leads/[id]:
□ Show real lead data
□ [Call Now] → Log call
□ [Send Email] → Send real email
□ [Schedule Meeting] → Save to calendar
□ [Send Proposal] → Generate PDF and send
□ All activities must save

/sales/activity:
□ Calendar must show real activities
□ [Check In] must save location
□ [Log Activity] must save

/sales/commission:
□ Calculate real commission from deals
□ Show breakdown
□ Payout history from database

═══════════════════════════════════════════════════════════════════
PART 4: AUTHENTICATION - MAKE IT BULLETPROOF
═══════════════════════════════════════════════════════════════════

USE SUPABASE AUTH FOR ALL USER TYPES:

Setup:
```bash
npm install @supabase/auth-helpers-nextjs
```

Create separate auth tables:
- customers (D2C users)
- sales_reps (sales team)
- wholesale_partners (B2B customers)
- admins (admin users)

IMPLEMENT:

1. Signup:
   - Hash passwords (Supabase does this)
   - Validate email format
   - Check if email exists
   - Send verification email
   - Create user record

2. Login:
   - Validate credentials
   - Create session
   - Set HTTP-only cookie
   - Redirect to dashboard

3. Forgot Password:
   - Send reset email (Resend)
   - Generate secure token
   - Token expires in 1 hour
   - Link goes to /reset-password?token=xxx

4. Reset Password:
   - Validate token
   - Check not expired
   - Update password
   - Invalidate token

5. Protected Routes:
   - Middleware checks auth
   - Redirect to login if not authenticated
   - Check user type (admin can't access /partner, etc.)

6. Session Management:
   - Sessions expire after 7 days
   - Refresh token automatically
   - [Sign Out] clears session

═══════════════════════════════════════════════════════════════════
PART 5: FORMS - ALL MUST WORK
═══════════════════════════════════════════════════════════════════

EVERY FORM must:

1. Validate inputs (client-side)
   - Required fields
   - Email format
   - Phone format
   - Min/max length
   - Show error messages

2. Submit to API (server-side)
   - Validate again
   - Save to database
   - Return success/error

3. Show feedback
   - Loading state (disable button, show spinner)
   - Success message (toast or alert)
   - Error message (toast or alert)

4. Reset or redirect
   - Clear form on success
   - Or redirect to relevant page

FORMS TO AUDIT:

□ Contact form
□ Newsletter signup
□ Product review form
□ Account signup
□ Account login
□ Forgot password
□ Reset password
□ Checkout form
□ Address form
□ Payment form
□ Lead creation form
□ Task creation form
□ Deal creation form
□ Campaign creation form
□ Message/email composer

USE:
- React Hook Form for validation
- Zod for schema validation
- Toast notifications (sonner or react-hot-toast)

═══════════════════════════════════════════════════════════════════
PART 6: EMAIL SYSTEM - ALL EMAILS MUST SEND
═══════════════════════════════════════════════════════════════════

SETUP RESEND:
```bash
npm install resend
```

CREATE EMAIL TEMPLATES:

1. Order Confirmation
2. Shipping Notification
3. Delivery Notification
4. Password Reset
5. Welcome Email (new customer)
6. Welcome Email (new partner)
7. Invoice Reminder (B2B)
8. Payment Receipt
9. Lead Assignment (sales rep)
10. Deal Won (sales rep)

EMAILS MUST TRIGGER:

□ Order placed → Order confirmation email
□ Order shipped → Shipping notification email
□ Order delivered → Delivery notification email
□ Password reset requested → Reset email
□ New account created → Welcome email
□ Invoice due soon → Reminder email
□ Payment received → Receipt email
□ Lead assigned to rep → Assignment email
□ Deal closed → Congratulations email

TEST ALL EMAILS:
- Send to your email
- Check they arrive
- Check formatting
- Check all links work
- Check unsubscribe works

═══════════════════════════════════════════════════════════════════
PART 7: PAYMENTS - STRIPE MUST WORK PERFECTLY
═══════════════════════════════════════════════════════════════════

STRIPE INTEGRATION:

1. D2C Checkout:
   - Use Stripe Checkout Sessions
   - Accept cards, Apple Pay, Google Pay
   - Save payment method for future orders
   - Handle successful payment
   - Handle failed payment
   - Create order in database
   - Send confirmation email

2. B2B Invoices:
   - Create Stripe Invoices
   - Send invoice link to partner
   - Accept card payments
   - Accept ACH/bank transfer
   - Track payment status
   - Send receipt email

3. Subscriptions:
   - Create Stripe Subscriptions
   - Recurring billing
   - Update/cancel subscription
   - Handle payment failures
   - Retry logic
   - Dunning emails

4. Webhooks:
   - Listen for payment events
   - Update order status
   - Send notifications
   - Handle refunds
   - Handle disputes

ENDPOINT: /api/webhooks/stripe
```typescript
import { headers } from 'next/headers';
import Stripe from 'stripe';

const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!);

export async function POST(req: Request) {
  const body = await req.text();
  const signature = headers().get('stripe-signature')!;
  
  try {
    const event = stripe.webhooks.constructEvent(
      body,
      signature,
      process.env.STRIPE_WEBHOOK_SECRET!
    );
    
    switch (event.type) {
      case 'checkout.session.completed':
        // Create order in database
        // Send confirmation email
        break;
      case 'payment_intent.succeeded':
        // Update order status
        break;
      case 'invoice.paid':
        // Update B2B invoice status
        // Send receipt
        break;
      // Handle other events
    }
    
    return Response.json({ received: true });
  } catch (err) {
    console.error('Webhook error:', err);
    return Response.json({ error: 'Webhook error' }, { status: 400 });
  }
}
```

TEST PAYMENTS:
- Use test card: 4242 4242 4242 4242
- Test declined card: 4000 0000 0000 0002
- Test 3D Secure: 4000 0027 6000 3184
- Verify order created
- Verify email sent
- Verify inventory decreased

═══════════════════════════════════════════════════════════════════
PART 8: DATABASE - ALL QUERIES MUST WORK
═══════════════════════════════════════════════════════════════════

RUN MIGRATIONS:
```bash
npx prisma migrate deploy
npx prisma generate
```

SEED DATABASE:
```bash
npm run seed
```

MUST HAVE REAL DATA:
□ 5+ products with images
□ 20+ customers
□ 10+ wholesale partners
□ 5+ sales reps
□ 50+ orders (mix of D2C and B2B)
□ 20+ leads
□ 10+ deals
□ 30+ tasks
□ 10+ invoices
□ 5+ marketing campaigns

ALL QUERIES MUST:
- Handle errors gracefully
- Show loading states
- Handle empty states ("No orders yet")
- Paginate large results
- Filter/search correctly
- Sort correctly

═══════════════════════════════════════════════════════════════════
PART 9: ERROR HANDLING - NEVER SHOW BROKEN PAGES
═══════════════════════════════════════════════════════════════════

IMPLEMENT:

1. Error Boundaries (React)
   - Catch component errors
   - Show friendly error page
   - Log to console/monitoring

2. API Error Handling
   - Try-catch all database queries
   - Return proper error responses
   - Show error toast to user

3. 404 Pages
   - Custom 404 page
   - "Page not found"
   - Link back to homepage

4. Loading States
   - Skeleton loaders for lists
   - Spinners for buttons
   - Progress bars for uploads
   - "Loading..." text

5. Empty States
   - "No orders yet" + [Start Shopping] button
   - "No leads yet" + [Add Lead] button
   - Helpful messaging, not just blank

═══════════════════════════════════════════════════════════════════
PART 10: FINAL PRE-LAUNCH CHECKLIST
═══════════════════════════════════════════════════════════════════

FUNCTIONALITY:
□ Every button works
□ Every form submits
□ Every link goes somewhere
□ All authentication flows work
□ All payment flows work
□ All email notifications send
□ All data loads from database
□ No placeholder content
□ No "coming soon" messages
□ No broken links

PERFORMANCE:
□ Images optimized
□ Page load < 3 seconds
□ No console errors
□ No console warnings (fix or suppress)

CONTENT:
□ Real product photos
□ Real product descriptions
□ Real pricing
□ Legal pages complete (Terms, Privacy, Refund, Shipping)
□ Contact info correct
□ Social links correct (or removed)

TESTING:
□ Test complete checkout flow (place real test order)
□ Test user signup/login
□ Test password reset
□ Test partner login
□ Test sales rep login
□ Test admin login
□ Test on mobile
□ Test on different browsers

DEPLOYMENT:
□ Environment variables set
□ Database migrated
□ Database seeded
□ Stripe in test mode (switch to live when ready)
□ Domain connected
□ SSL working

═══════════════════════════════════════════════════════════════════
START NOW - WORK IN THIS ORDER
═══════════════════════════════════════════════════════════════════

HOUR 1: Critical Functionality (60 min)
1. Sales rep auth pages (15 min)
2. Fix all non-working buttons on homepage/shop/cart/checkout (30 min)
3. Make checkout actually work end-to-end (15 min)

HOUR 2: Dashboards & Messages (60 min)
4. Add messages system to all dashboards (30 min)
5. Fix all dashboard buttons to actually work (30 min)

After 2 hours:
- Site is functional
- Can actually take orders
- No broken buttons
- Authentication works
- Messages work

Then you can launch!

Additional polish:
- Add Gmail integration (optional, can do post-launch)
- Optimize performance
- Add more features

═══════════════════════════════════════════════════════════════════

BEGIN NOW. Fix everything. Make it perfect. We launch in 2 hours.

Report back after each major section is complete.

GO! 🚀