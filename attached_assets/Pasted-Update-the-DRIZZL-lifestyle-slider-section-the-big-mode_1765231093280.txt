Update the DRIZZL lifestyle slider section (the big model photos).

GOAL
- Make the cards smaller and more “normal” visually (not full-page height).
- Limit the vertical height of the section.
- Keep a horizontal film-strip with multiple images visible at once.
- Allow scrolling horizontally with:
  a) mouse wheel / trackpad drag
  b) left/right arrow buttons.

1) CONSTRAIN SECTION HEIGHT

Reduce the height of the entire section:

.lifestyle-section {
  padding: 3rem 0;
  background: #000;       /* keep current background */
}

.lifestyle-track {
  display: flex;
  gap: 24px;
  overflow-x: auto;
  overflow-y: hidden;
  padding: 0 4vw 1.5rem;
  scroll-snap-type: x mandatory;
  -webkit-overflow-scrolling: touch;
}

/* Each slide/card – much smaller now */
.lifestyle-card {
  position: relative;
  flex: 0 0 40vw;      /* was too big; reduce width */
  max-width: 520px;    /* cap actual size */
  aspect-ratio: 4 / 5; /* portrait, but not full screen */
  border-radius: 24px;
  overflow: hidden;
  background: #ffffff;
  scroll-snap-align: center;
}

/* Image inside card */
.lifestyle-card img {
  width: 100%;
  height: 100%;
  object-fit: cover;   /* or 'contain' if you want full body */
  display: block;
}

/* Mobile tweak: slightly wider but still smaller than viewport */
@media (max-width: 768px) {
  .lifestyle-card {
    flex: 0 0 70vw;
    max-width: none;
  }
}

2) HORIZONTAL SCROLL WITH MOUSE WHEEL

Add JS that converts vertical wheel into horizontal scroll on the .lifestyle-track:

const track = document.querySelector('.lifestyle-track');

if (track) {
  track.addEventListener(
    'wheel',
    (e) => {
      if (Math.abs(e.deltaY) > Math.abs(e.deltaX)) {
        e.preventDefault();
        track.scrollLeft += e.deltaY;   // scroll sideways
      }
    },
    { passive: false }
  );
}

3) ARROW BUTTONS

Assume we have two arrow buttons in the markup:

<button className="lifestyle-arrow prev">‹</button>
<button className="lifestyle-arrow next">›</button>

Style them minimally:

.lifestyle-arrow {
  position: absolute;
  bottom: 1.5rem;
  right: 1.5rem;
  z-index: 5;
  width: 40px;
  height: 40px;
  border-radius: 999px;
  border: 1px solid #fff;
  background: rgba(0,0,0,0.7);
  color: #fff;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
}

/* Place prev on the left side */
.lifestyle-arrow.prev {
  right: 4.5rem;
}

JS to make arrows scroll the track:

const prevBtn = document.querySelector('.lifestyle-arrow.prev');
const nextBtn = document.querySelector('.lifestyle-arrow.next');
const card = document.querySelector('.lifestyle-card');

if (track && card) {
  const step = card.getBoundingClientRect().width + 24; // card width + gap

  if (nextBtn) {
    nextBtn.addEventListener('click', () => {
      track.scrollBy({ left: step, behavior: 'smooth' });
    });
  }

  if (prevBtn) {
    prevBtn.addEventListener('click', () => {
      track.scrollBy({ left: -step, behavior: 'smooth' });
    });
  }
}

4) CLEANUP

- Ensure the lifestyle section does NOT use height: 100vh or similar anywhere.
- Remove any extra padding or margins that make the card appear too tall.
- Final result: on desktop you see ~1.5–2 cards at a time at a comfortable height (around ~450–520px), with horizontal scrolling via mouse and via arrows.
