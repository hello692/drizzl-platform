You are my senior UI engineer.

Redesign the homepage FIRST hero section + header to look like the Louis Vuitton homepage hero experience — BUT original to DRIZZL.

REFERENCE STYLE (do not copy assets, only layout + behavior):
https://us.louisvuitton.com/eng-us/homepage

FIX AND REQUIREMENTS:

⭐ 1. HEADER STYLE MATCH
- Remove the opaque black background from the top navigation.
- Make it transparent and overlay on top of the hero.
- Convert logo + icons to WHITE.
- Spacing must match LV: logo centered horizontally, menu & search left, profile & cart right.
- Clean minimal icons similar to LV style (line iconography, thin strokes).

HEADER CSS BEHAVIOR:
header {
  position: absolute; /* overlays hero */
  width: 100%;
  top: 0;
  z-index: 50;
  color: white;
  background: transparent !important;
  padding: 24px 48px;
}

All icons, nav links, language selector must be white.  
Hover states: 50% opacity fade.

⭐ 2. HERO SECTION REDESIGN
- Remove “i am fueled” style.
- Match LV layout: text is positioned lower (lower 30% region), not centered vertically.
- Ensure readability with subtle dark gradient from bottom.
- NO dark vignette at the top.

HERO CONTENT PLACEMENT (like LV):
<section class="hero">
  <video autoplay muted loop playsinline ... />
  <div class="hero-overlay"></div>

  <div class="hero-copy">
    <p class="hero-kicker">DISCOVER</p>
    <h1 class="hero-title">The Smoothie Season with DRIZZL</h1>
    <p class="hero-sub">Smoothies for people who want to feel their best.</p>

    <div class="hero-cta-row">
      <a class="hero-btn primary">Shop Smoothies</a>
      <a class="hero-btn secondary">Feel Your Best</a>
    </div>
  </div>
</section>

⭐ 3. HERO POSITIONING + TYPOGRAPHY RULES

.hero {
  width: 100%;
  height: 100vh;
  position: relative;
  overflow: hidden;
}

.hero video {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.hero-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    to bottom,
    rgba(0,0,0,0.0) 0%,
    rgba(0,0,0,0.50) 85%
  );
}

.hero-copy {
  position: absolute;
  bottom: 18%;
  left: 50%;
  transform: translateX(-50%);
  text-align: center;
  color: white;
  width: 100%;
  max-width: 680px;
  padding: 0 20px;
}

.hero-kicker {
  letter-spacing: 0.22em;
  font-size: 0.70rem;
  text-transform: uppercase;
  opacity: 0.85;
  margin-bottom: 12px;
}

.hero-title {
  font-size: 2.6rem;
  font-weight: 500;
  margin-bottom: 12px;
}

.hero-sub {
  opacity: 0.85;
  font-size: 1rem;
  margin-bottom: 28px;
}

.hero-cta-row {
  display: flex;
  gap: 16px;
  justify-content: center;
}

.hero-btn {
  padding: 0.75rem 1.75rem;
  border-radius: 999px;
  font-size: 0.9rem;
  border: 1px solid white;
  color: white;
  transition: background 0.2s, color 0.2s, transform 0.2s;
}

.hero-btn.primary {
  background: white;
  color: black;
}

.hero-btn.primary:hover {
  transform: translateY(-1px);
}

.hero-btn.secondary:hover {
  background: rgba(255,255,255,0.17);
}

⭐ 4. RESPONSIVE BEHAVIOR (like LV)

@media (max-width: 768px) {
  .hero-title { font-size: 1.8rem; }
  .hero-copy { bottom: 14%; max-width: 420px; }
  .hero-cta-row { flex-direction: column; }
}

⭐ 5. APPLY CHANGES:
- Replace your current hero markup + CSS with this new structure.
- Update your header styling to match this transparent/white behavior.
- Remove the black header background from the top of the DRIZZL site.
- Push logo and icons to white variants.

End result must visually feel like Louis Vuitton hero aesthetic:
– transparent overlay header  
– white icons  
– clean modern type  
– cinematic breathing space  
– hero text positioned low  

Implement this exactly in React + CSS module form consistent with my existing setup.
